name: Android Build

on:
  push:
    branches: [ main, master ]

permissions: {}
jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up JDK 17
      uses: actions/setup-java@v1
      with:
        java-version: 17
    
    - name: Set up Android SDK
      uses: android-actions/setup-android@v2
      with:
        cmdline-tools-version: '9.0'
        api-level: '36'
        build-tools-version: '36.0.0'
    
    - name: Accept Android SDK licenses
      run: |
        yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    
    - name: Build APK with Gradle
      run: ./gradlew android:assembleDebug
      env:
        ANDROID_HOME: ${{ env.ANDROID_SDK_ROOT }}
    
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-debug-apk
        path: android/build/outputs/apk/debug/
        retention-days: 30
    
    # Optional: For testing on a real device or emulator
    # - name: Setup ADB
    #   uses: shyim/setup-android-adb-action@v1
    
    # - name: Install and run on connected device (if available)
    #   run: |
    #     adb devices
    #     ./gradlew android:installDebug android:run
    #   if: ${{ false }}  # Disabled by default, enable if you have tests with emulator
    
    # Optional: Build release APK (requires signing configuration)
    # - name: Build Release APK
    #   run: ./gradlew android:assembleRelease
    #   env:
    #     ANDROID_HOME: ${{ env.ANDROID_SDK_ROOT }}
    
    # - name: Upload Release APK artifact
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: android-release-apk
    #     path: android/build/outputs/apk/release/
    #     retention-days: 30